name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - name: checkout
      uses: actions/checkout
    
    - name: check-licenses
      uses: ./.github/actions/golang
      run: make check-licenses
